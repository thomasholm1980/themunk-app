[build]
  base    = "apps/web"
  command = "npm run build"
  publish = ".next"

[dev]
  command    = "npm run dev"
  port       = 3000
  targetPort = 3000

[functions]
  directory    = "netlify/functions"
  node_bundler = "esbuild"
